@model IEnumerable<Proyectof.Models.Habitaciones>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.numero)
        </th>
        <th>
            Tipo
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipos.descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idHabitacion }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idHabitacion }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.idHabitacion })
        </td>
    </tr>
}

</table>
